import cart from "./assets/cart.svg";

const CartWidget = () => {
  return (
    <div style={{ color: "white" }}>
      <img src={cart} alt="cart-widget" height="50px" /> 0
    </div>
  );
};

export default CartWidget;
